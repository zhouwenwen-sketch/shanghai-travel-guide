# 旅游项目启动指南

## 1. 启动后端 (Spring Boot + MySQL)

```bash
cd my-project/travel-backend
./mvnw spring-boot:run
```

- 端口: `http://localhost:8081`
- 依赖: MySQL (`localhost:3306`)，数据库 `travel_db`，用户 `root`，密码 `123456`

## 2. 启动前端 (Vue 3 + Vite)

```bash
cd my-project
npm run dev
```

- 端口: `http://localhost:5173`
- 修改前端代码后保存即自动热更新，无需重启

## 3. 注意事项

- **修改 Java 代码后**，需要重启后端命令
- **修改前端代码后**，如果热更新没生效，按 `Ctrl + Shift + R` 强制刷新浏览器
- **后端启动失败**，前端会自动降级使用 mock 数据，不影响开发和调试

## 4. 常用命令

| 操作 | 命令 |
|------|------|
| 启动后端 | `cd travel-backend && ./mvnw spring-boot:run` |
| 启动前端 | `cd my-project && npm run dev` |
| 编译后端（不启动） | `cd travel-backend && ./mvnw compile` |
| 强制刷新浏览器 | `Ctrl + Shift + R` |
