### 项目相关
## 0.自我介绍
面试官您好，我叫周文雯，是上海大学计算机科学与技术专业的一名大三学生，预计明年毕业，暑期实习可保证一周5天，连续三个月以上，去年暑假做过一段实习，主要负责html邮件的编写，官网维护数据更新之类的，今年寒假我独立开发了一个前端项目，该项目有三个亮点，第一是用 Pinia + localStorage 双写实现了刷新页面不丢登录态，第二是配置了路由守卫来控制页面访问权限，第三是用 Element Plus 的暗黑模式变量来实现深浅主题的切换，我常用的ai编程工具有cursor、Claude Code、chatgpt等，我打算用ai辅助编程将这个项目完善为一个全栈项目
## 1.介绍一下你的项目
> 这是一个基于 Vue3 的上海旅游攻略单页应用。
> 技术栈用了 Vue3 Composition API setup语法+ Pinia + Vue Router + Element Plus + Vite。
> 我主要负责：页面组件开发、登录注册的前端模拟、深色模式切换、路由配置和守卫。
> 项目亮点是：
1. 用 Pinia + localStorage 双写实现了刷新不丢登录态，
2. 用路由守卫控制页面访问权限，
3. 用 Element Plus 的暗黑模式变量实现了深色切换。
## 1.2为什么选 Vue3 + Pinia + Element Plus 这个技术栈？
> 首先说 Vue3。因为我是独立开发这个项目，Vue3 的 Composition API 写法
> 能让我把一个功能相关的变量和方法写在一起，而不是像老写法那样
> 数据放一堆、方法放一堆，改一个功能要在文件里到处翻。
> 再加上 Vite 这个构建工具启动非常快，改代码刷新几乎无延迟。
>
> 其次是 Pinia。我项目里有登录状态和主题模式两个数据需要在至少三四个
> 组件之间共用 —— 比如顶部导航要读用户是否登录、左侧菜单要读当前主题。
> Pinia 是 Vue 官方推荐的状态管理库，相比之前的 Vuex 更轻量：
> 不用写 mutations 这个中间环节，直接改状态就行，逻辑更直。
>
> 最后是 Element Plus。它提供了菜单、卡片、轮播图这些现成组件，
> 不用我从零手写 UI。更重要的是它内置了暗黑模式方案：
> 我只需要给 html 根节点加一个 dark 的 class，组件库内部
> 所有元素会自动切换到暗色配色，省了我大量写样式的时间。
## 2.Composition API怎么组织代码
Composition API是vue3引入的新写法，它不像options api那样按照类型data/metheds/computed分散代码，而是按照功能，把同一功能的逻辑代码聚合在一起，从而更好地维护和逻辑复用。比如我的项目里的头部导航栏组件分了三块，一块是路由相关逻辑，引入路由实例，一块是用户状态相关，从pinia取出登录态和用户名，一块是交互逻辑，菜单的一些渲染，这些都将操作数据和相关方法放在一块
## 3.setup语法糖
Composition API 的语法糖，省掉了 setup() 函数和 return，
顶层变量自动暴露给模板，代码更简洁。
## 4.路由守卫怎么用的
用前置守卫实现了两件事：
已经登录的用户再访问登录页会自动重定向到首页，
未登录的用户若要访问被meta标记需要登录的页面，守卫就会自动拦截，跳转到登录页，并通过query参数记住原本想去的页面路径，登陆后可以直接跳转。
## 4.2路由是怎么设计的
> 路由这块做了两个层面。
> 第一个层面是路由规则本身：我配了两个页面路径，
> 一个首页一个登录页，都用了懒加载——也就是用户访问到那个页面时
> 才去下载对应的代码文件，而不是一打开网站就把所有页面代码都下完，
> 这样首屏加载更快。
>
> 第二个层面是路由守卫，就是页面跳转前的一道检查：
> 如果用户已经登录了还去访问登录页，就直接重定向到首页；
> 如果某个页面标记了"需要登录才能看"但用户还没登录，
> 就跳去登录页，同时用浏览器的查询参数记住用户原本想去哪，
> 登录成功后可以直接跳回去。目前首页没要求登录，
> 但框架已经写好了，将来加新页面只需要多一行标记就能启用拦截。
## 5.pinia怎么管理跨组件状态
我的项目里实现了Pinia和lacalstorage双写，首先两者都是集中式状态存储管理，但是Pinia是运行在浏览器内存里的，它本质上是vue响应式系统里的一个全局对象，所有的组件都能共享它里面的数据，但它有个问题就是页面刷新之后js运行环境会被销毁，内存全部释放，里面的数据也就没了。而localStorage是浏览器提供的独立存储空间，存在硬盘上，永久存储，但它只能存字符串，而且不是响应式的。比如登录成功后，token会同时存到Pinia 和 localStorage，页面正常使用时，所有组件从 Pinia 读，享受响应式；用户刷新后，在应用初始化的地方从 localStorage 读回 token 填到 Pinia 里，用户感觉登录态没断。登出的时候两边一起清。封装一层统一的 action 来保证两边一致性就行了。
> 我项目里有两个store——用户的和主题的。以用户store为例：
>
> 第一层是 state，也就是数据本身：存了是否已登录、当前用户名。
> 关键是初始化时我会先从浏览器的 localStorage 里读值，
> 所以用户刷新页面后登录状态还在，不会掉。
>
> 第二层是 getters，也就是计算属性：比如给模板用的展示名，
> 有用户名就显示用户名，没有就显示"游客"。
> 这样模板直接读一个字段就行，不用每次判断一遍。
>
> 第三层是 actions，也就是修改数据的方法：
> 登录方法做了一个合并处理——用户名第一次出现就自动注册到 localStorage，
> 再次出现就核对密码是否一致。退出方法就是清掉 store 里的状态同时擦掉 localStorage。
>
> 跨组件共用的流程是：任何需要用户状态的组件，
> 引入这个 store 然后从中取出需要的字段就行。
> 比如顶部导航要判断显示"请登录"还是"你好某用户"，
> 登录页要调登录方法改状态。
> 因为 Vue 的响应式机制，一个地方改了状态，所有引用的组件自动更新 UI，
> 不需要手动通知。
## 6.页面优化的想法
1. 路由懒加载(我的项目里用到了)，进入页面再加载，减少首屏
2. 图片尽量压缩
3. 利用缓存、防抖、节流，例如搜索框：输入时做 debounce，避免每输入一次都请求接口。
4. 用虚拟 DOM 来帮助做性能优化
## 如何使用ai
先把我所有的需求想法最终目标告诉chat模型，帮我做个分析，让他理解我要做什么，然后跟他说如果我要完成任务还需要补充哪些背景知识，基于你的知识帮我生成一份结构清晰逻辑严密的system prompt，如果是大任务先让他一步步拆解任务，一步步分析，分成小模块，让他生成有思维链的prompt，再把prompt交给专业的ai，比如cursor，在它一步步生成完后去看是否符合要求从而进行微调

## prompt编写优化
1. 设定角色
2. 加入约束条件
2. 提供示例
3. 做强调
4. 构建清晰的思维链，拆解复杂任务步骤
## 6.原型、原型链
每个函数都有一个prototype属性，称之为原型，因为它的值是一个对象，所以也叫原型对象，原型的作用有：
1. 原型可以放一些属性和方法，提供给实例化对象使用。
2. 原型可以做继承。
每个对象都有个_proto_属性，它指向该对象的原型对象，因为原型对象也是个对象，它也有_proto_属性，它又会指向该原型对象的原型对象，这样一层一层形成的链式结构就称为原型链，最顶层找不到就返回null
## 7.微任务、宏任务、事件循环
js是单线程的，为了防止阻塞代码，把代码（任务）分成同步和异步，同步代码交给js引擎，异步代码交给宿主环境（浏览器、node等支持多线程），同步代码放在执行栈立即执行，异步代码等待相应时间后送到任务队列里，而又把异步代码（任务）分成宏任务和微任务，有了promise后js引擎自身也能发起异步任务了，也就是微任务，所以执行流程就是先清空执行栈里的所有同步代码，接着查看微任务队列，把微任务推送到执行栈，清空微任务，查看宏任务队列，取出一个宏任务，清空执行栈，清空微任务队列，这个循环过程就叫事件循环
## 8.vue3生命周期
常用的setup → → onMounted → → onUpdated → → onUnmounted
## 9.v-if 和 v-show 
v-if 和 v-show 都能控制元素的显隐，但实现原理不同。v-if 是真正的条件渲染，条件为 false 时元素从 DOM 中完全移除，切换时会触发组件的销毁和重建，有较大的切换开销。v-show 无论条件如何都会渲染元素，只是通过 CSS 的 display 属性切换显隐，有初始渲染开销但没有切换开销。
频繁切换的场景用 v-show，条件很少改变或需要配合 v-else 的场景用 v-if。
## 10.选择器优先级
!important > 行内样式 > ID 选择器 > 类 / 伪类 > 标签选择器
## 11.闭包是什么？有什么用？
函数嵌套，内层函数访问外层函数变量，可以延长变量生命周期，私有化变量
## 12.computed 和 methods 
computed 和 methods 的核心区别在于缓存。computed 是基于响应式依赖的，依赖变化才重新计算，否则返回缓存值，适合做列表过滤、总价计算这种依赖其他数据的场景。methods 每次调用都会执行，适合不需要缓存的场景，比如事件处理函数，或者需要传参的计算。
## 13.组件通信方式
组件通信就是组件之间传递数据和消息。Vue 中有三种常用方式：
父传子用 props，父组件在子组件标签上绑定数据，子组件通过 defineProps 接收，数据流向是单向的。
子传父用 emit，子组件通过 $emit 触发自定义事件，父组件用 @事件名 监听响应。
跨层级或兄弟组件通常用 Pinia 状态管理库，它像一个全局广播站，任何组件都可以读写 store 里的数据，数据变了所有引用处自动更新。
## 14.双向绑定事件
核心原理是：通过响应式系统实现数据到视图的自动更新；通过事件监听实现视图到数据的同步。
具体到 v-model 指令，它本质上是一个语法糖。在普通输入框上，它等价于 :value 绑定数据到视图，加上 @input 事件监听用户输入并更新数据。不同表单元素对应不同属性和事件，但原理相同，都是一个属性加一个事件。
## 15.事件冒泡
事件冒泡是指子元素上触发的事件会一层一层向上传播到父元素，直到根节点。这是 DOM 事件模型的默认行为。
实际开发中，有时需要阻止冒泡，可以用 @click.stop 修饰符，常见于弹窗场景——点击弹窗内部不让它关闭，只允许点背景关闭。反过来，也可以利用冒泡做事件委托，把子元素的事件统一绑定在父元素上，减少事件绑定数量，提高性能。
## 16.事件委托
事件委托是利用事件冒泡机制的一种优化技巧。不给每个子元素单独绑定事件，而是在共同的父元素上绑定一个事件监听器，通过 event.target 判断实际触发事件的子元素，再做相应处理。
好处有三个：减少事件绑定数量，提升性能；新增的子元素自动被父元素的事件覆盖，不需要手动再绑定；逻辑集中一个函数，便于维护。
## 17.canvas
它是 HTML5 的画布元素，可以用 JS 动态绘制图形，适合做游戏或特效
## 18.盒模型、BFC
标准盒模型：width 只算 content
怪异盒模型（box-sizing: border-box）：width = content + padding + border（文字+内边距+边框）
现在开发中几乎都用 border-box，好算尺寸
清除内部浮动，解决外边距重叠
## 19.this 指向
普通独立函数：谁调用它 this 指向谁
对象方法：this 指向调用它的对象
构造函数：this 指向new 出来的实例
箭头函数：没有自己的 this，继承外层作用域的 this
>call、apply、bind可以主动改 this
## 20.什么是DOM？什么是BOM？
DOM 叫文档对象模型，就是浏览器把 HTML 文档解析成一组 JavaScript 可操作的对象，每个标签都会对应一个 DOM 对象
BOM 叫浏览器对象模型，主要操作窗口window、地址栏location、历史记录history这些
## 21.虚拟DOM是什么
因为直接操作真实 DOM 成本比较高，为了提高页面更新的效率，在react/vue里引入了虚拟DOM，虚拟 DOM 就是用 JS 对象描述页面，通过 diff 算法找到与真实DOM之间的最小变化，再去更新真实 DOM，从而减少不必要操作
## 22.跨域是什么？怎么解决
跨域是浏览器的同源策略导致的，前后端协议、域名、端口任意一个不同都会触发，浏览器就会拦截
常见解决方案有两种：一种是后端配置 CORS，在响应头里允许指定来源访问；另一种是前端开发时通过代理服务器转发请求，比如 Vite 的 proxy(不是vue3里js内置对象用来代理和监听对象实现响应式的 Proxy)
## 23.HTTP 请求过程
>输入URL->DNS解析->建立连接->浏览器发送请求->服务器处理->返回响应->浏览器解析页面
用户输入 URL 后，浏览器会先通过 DNS 把域名解析成 IP。然后通过 TCP 三次握手建立连接，如果是 HTTPS 还会进行 TLS 握手。连接建立后浏览器发送 HTTP 请求，请求包含请求行、请求头和请求体。服务器收到后执行业务逻辑并查询数据库，再返回响应，里面有状态码、响应头和响应体。浏览器拿到响应后解析 HTML 和 CSS，生成 DOM 树并渲染页面，最后连接关闭或者复用。
## vue3相比vue2做了哪些改变
1. Composition API vs Options API
2. 响应式系统的重构，Vue 3用 Proxy代理整个对象，任何属性的增删改都能检测到，性能更好、功能更完备
3. 打包工具vite
3. 更好的 TypeScript 支持
4. 编译优化：Vue 3 在编译时标记静态节点和动态节点，更新时只对比动态内容，比 Vue 2 的虚拟 DOM Diff 快得多
5. 生命周期改名
## AJAX（Asynchronous JavaScript and XML）
意思是不刷新整个网页，就能在背后发请求、拿数据、更新页面
## typescript中type和interface的区别
type 和 interface 都可以定义对象结构，区别主要有三点。
1. interface 支持声明合并，如果定义两个同名的 interface，它们会自动合并成一个；type 不支持，同名会报错。
2. 扩展的写法不同，interface 用 extends 关键字，type 用交叉类型 &。
3. type 更灵活，可以定义联合类型、元组、基本类型的别名，这些 interface 都做不了。
实际开发中，如果是写开源库或者定义对外暴露的 API，我会优先用 interface，因为声明合并的特性对使用者更友好；如果是项目内部的类型，或者需要用到联合类型、工具类型，我就用 type。



### 手写代码题
## 手写call、apply、bind强行指定一个函数的this指向谁
# 1.call(指定的this,一系列参数)
Function.prototype.myCall=function(context,...args){
    context=context||window;
    const key=Symbol();
    context[key]=this;
    const res=context[key](...args);
    delete context[key];
    return res;
}
Function.prototype.myCall=function(context,...args){
    context=context||window;
    const key=Symbol();
    context[key]=this;
    const res=context[key](...args);
    delete context[key];
    return res;
}
# 2.apply
Function.prototype.myApply=function(context,args){
    context=context||window;
    const key=Symbol();
    context[key]=this;
    const res=context[key](...args);
    delete context[key];
    return res;
}
# 3.bind
Function.prototype.myBind=function(context,...args1){
    const fn=this;
    return function(...args2){
        return fn.call(context,...args1,...args2);
    };
}
Function.prototype.myBind=function(context,...args1){
    const fn=this;//把原函数存起来
    return function(...args2){//返回新函数
        return fn.call(context,...args1,...args2);//调用时才返回结果
    };
}
## 手写数组方法map、filter、reduce
# 4.map
Array.prototype.myMap=function(fn){
    let res=[];
    for(let i=0;i < this.length;i++){
        res.push(fn(this[i],i,this));
    }
    return res;
}
Array.prototype.myMap=function(fn){
    let res=[];
    for(let i=0;i < this.length;i++){
        res.push(fn(this[i],i,this));
    }
    return res;
}
# 5.filter
Array.prototype.myFilter=function(fn){
    let res=[];
    for(let i=0;i < this.length;i++){
        if(fn(this[i],i,this))
        res.push(this[i]);
    }
    return res;
}
Array.prototype.myFilter=function(fn){
    let res=[];
    for(let i=0;i < this.length;i++){
        if(fn(this[i],i,this))
        res.push(this[i]);
    }
    return res;
}
# 6.reduce
Array.prototype.myReduce=function(fn,init){
    let acc=init;
    let start=0;
    if(init===undefined){
        acc=this[0];
        start=1;
    }
    for(let i=start;i < this.length;i++){
        acc=fn(acc,this[i],i,this);
    }
    return acc;
}
Array.prototype.myReduce=function(fn,init){
    let acc=init;
    let start=0;
    if(init===undefined){//没有初始值就从第一个元素开始
        acc=this[0];
        start=1;
    }
    for(let i=start;i < this.length;i++){
        acc=fn(acc,this[i],i,this);
    }
    return acc;
}
## 手写防抖、节流
# 7.防抖
function debounce(fn,ms){
    let timer=null;
    return function(...args){
        if(timer) clearTimeout(timer)
        timer=setTimeout(()=>{
            fn.apply(this,args);
        },ms)
    }
}
function debounce(fn,ms){
    let timer=null;
    return function(...args){
        if(timer) clearTimeout(timer);
        timer=setTimeout(()=>{
            fn.apply(this,args);
        },ms)
    }
}
# 8.节流
function throttle(fn,ms){
    let timer=null;
    return function(...args){
        if(timer) return;
        timer=setTimeout(()=>{
            fn.apply(this,args);
            timer=null;
        },ms)
    }
}
## 手写数组扁平化、去重、排序
# 9.flat
function myFlat(arr){
    let res=[];
    for(let item of arr){
        if(Array.isArray(item))
        res=res.concat(myFlat(item));
    }
    return res;
}
function myFlat(arr){
    let res=[];
    for(let item of arr){
        if(Array.isArray(item)){
            res=res.concat(myFlat(item));
        }
    }
    return res;
}
# 10.uniq
function uniq(arr){
    let res=[];
    for(let item of arr){
        if(!res.includes(item))
        res.push(item);
    }
    return res;
}
function uniq(arr){
    let res=[];
    for(let item of arr){
        if(!res.includes(item)){
            res.push(item);
        }
    }
    return res;
}
# 11.排序
.sort((a,b)=>a-b)
## 手写递归深拷贝deepClone
function deepClone(obj){
    if(obj===null||typrof obj!=='object')
    return obj;
    let res=Array.isArray(obj)?[]:{};
    for(let key in obj){
        res[key]=deepClone(obj[key]);
    }
    return res;
}
function deepClone(obj){
    if(obj===null||typeof obj!=='object')
    return obj;
    let res=Array.isArray(obj)?[]:{};
    for(let key in obj){ //普通对象没有length属性，
        res[key]=deepClone(obj[key]);
    }
    return res;
}




